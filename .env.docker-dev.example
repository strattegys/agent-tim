# Optional: copy lines you need into COMMAND-CENTRAL/.env (same folder as docker-compose.dev.yml).
# Docker Compose reads .env for ${VAR} substitution in compose files only (not Next.js).

# --- Direct CRM from dev container (no TCP bridge on Windows) ---
# Uncomment if `docker compose -f docker-compose.dev.yml exec web npm run check-crm-db` succeeds.
# Confirm CC tailnet IP in PROJECT-MEMORY.md section 3 if this stops working.
# CC_DOCKER_CRM_DB_HOST=100.74.54.12
# CC_DOCKER_CRM_DB_PORT=5432

# --- Default: leave CC_DOCKER_* unset; use host.docker.internal + bridge from dev-docker-up.ps1 ---
