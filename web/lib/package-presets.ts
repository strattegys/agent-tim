/**
 * Canonical name for the seeded agent-builder draft row (`templateId: custom`, empty deliverables).
 * Add workflows from Package Kanban → package overlay → Add workflows.
 */
export const AGENT_BUILDER_LAUNCH_PACKAGE_NAME = "Agent Builder Launch";
